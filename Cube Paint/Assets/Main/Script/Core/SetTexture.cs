using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SetTexture : MonoBehaviour
{
    // Start is called before the first frame update
    Texture[] texture;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //private void Reset()
    //{
    //    var a = GetComponent<SwitchMaterialColor>();

    //    texture = a.sprite;
        

    //    GameObject[] objects = GameObject.FindGameObjectsWithTag("Wall");

    //    foreach (var obj in objects)
    //    {
    //        obj.GetComponent<MeshRenderer>().material.SetTexture("_MainTex", texture[]);
    //    }


    //    GameObject[] objects1 = GameObject.FindGameObjectsWithTag("notPaintTag");

    //    foreach (var obj in objects1)
    //    {
    //        obj.GetComponent<MeshRenderer>().material.SetTexture("_MainTex", texture[]);
    //    }

    //}
}
